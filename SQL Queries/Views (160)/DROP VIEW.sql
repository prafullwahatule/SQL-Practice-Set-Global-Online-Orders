-- =============================================
-- DROP VIEW
-- Definition:
-- DROP VIEW is used to delete an existing view from the database.
-- Once dropped, the view and its definition are removed, but underlying tables remain unaffected.
-- =============================================

-- 1. Drop a view showing all products out of stock.

-- 2. Drop a view showing employees born before 1960.

-- 3. Drop a view showing orders shipped by Federal Shipping.

-- 4. Drop a view showing products needing reorder (UnitsInStock <= ReorderLevel).

-- 5. Drop a view showing customers who have not placed any orders.

-- 6. Drop a view showing top 5 most expensive products.

-- 7. Drop a view showing orders along with product names and quantities.

-- 8. Drop a view showing suppliers who supply products in Condiments category.

-- 9. Drop a view showing products with low units in stock (<10).

-- 10. Drop a view showing orders with total price per order.
