-- =============================================
-- WINDOW FUNCTION: Moving averages, Running total, Rolling sums
-- =============================================

-- 1. Calculate the running total of order amounts for each customer ordered by OrderDate.

-- 2. Compute the running total of quantities for each product across all orders.

-- 3. For each employee, calculate the running total of Freight for orders handled.

-- 4. For each customer, calculate the moving average of order amounts for the last 3 orders.

-- 5. For each product, calculate the moving average quantity ordered over the last 5 orders.

-- 6. Compute the running total UnitsInStock for products in each category ordered by ProductID.

-- 7. For each supplier, calculate the rolling sum of UnitsOnOrder sorted by ProductID.

-- 8. For each customer, calculate the cumulative total quantity purchased over time.

-- 9. For each employee, calculate the moving average of total order amount over the last 3 orders.

-- 10. For each product, calculate the running sum of revenue (UnitPrice * Quantity) across all order_details.

-- 11. For each customer, calculate the cumulative average order amount.

-- 12. For each category, calculate the running total UnitsOnOrder for products sorted by UnitPrice.

-- 13. For each employee, calculate the rolling sum of quantities ordered for orders handled.

-- 14. Compute the running total of Freight per shipper over time.

-- 15. For each customer, calculate the moving average order quantity for the last 3 orders.

-- 16. For each product, calculate the cumulative sum of UnitsOnOrder.

-- 17. For each employee, calculate the running total revenue from the orders handled.

-- 18. For each customer, calculate the rolling sum of order amounts for 3 consecutive orders.

-- 19. For each category, calculate the cumulative revenue of all products sorted by ProductID.

-- 20. For each product, calculate the moving average revenue over last 5 orders.

-- 21. For each supplier, calculate the running total of UnitsInStock.

-- 22. For each customer, calculate the rolling sum of quantities ordered over last 4 orders.

-- 23. For each employee, calculate the cumulative sum of Freight handled.

-- 24. For each shipper, calculate the running total of orders shipped.

-- 25. For each category, calculate the moving average UnitPrice over products sorted by ProductID.

-- 26. For each product, calculate the rolling sum of quantities over last 3 order_details.

-- 27. For each customer, calculate the cumulative sum of total revenue from orders.

-- 28. For each employee, calculate the moving average order amount over last 5 orders.

-- 29. For each shipper, calculate the cumulative sum of Freight per month.

-- 30. For each product, calculate the running total UnitsInStock + UnitsOnOrder as rolling inventory.

