-- =============================================
-- WINDOW FUNCTION: DENSE_RANK()
-- Assigning dense ranks based on orderings and partitions
-- =============================================

-- 1. Rank orders by total amount using DENSE_RANK() (highest first).

-- 2. For each customer, rank their orders by OrderDate (earliest first).

-- 3. For each employee, rank their orders by OrderAmount (largest first).

-- 4. Rank products within each category by UnitPrice (highest first).

-- 5. For each product, rank order_details by Quantity (highest first).

-- 6. Rank suppliers by number of products supplied (most first).

-- 7. Rank customers by total number of orders placed.

-- 8. For each shipper, rank their orders by Freight (highest first).

-- 9. Rank categories by total products sold.

-- 10. For each employee, rank orders by Quantity of items in the order.

-- 11. Rank customers by total spending (sum of all orders).

-- 12. For each supplier, rank products by UnitsInStock (highest first).

-- 13. Rank employees by total number of orders handled.

-- 14. For each product, rank orders by UnitPrice.

-- 15. Rank customers in each country by total order amount.

-- 16. For each category, rank products by UnitsOnOrder.

-- 17. Rank suppliers by total sales value of their products.

-- 18. For each shipper, rank orders by order date (oldest first).

-- 19. Rank products across all categories by total quantity sold.

-- 20. For each customer, rank orders by total quantity purchased.

-- 21. Rank employees by total revenue handled from orders.

-- 22. For each category, rank products by total sales value (UnitsInStock * UnitPrice).

-- 23. Rank suppliers by total UnitsOnOrder across their products.

-- 24. For each customer, rank products ordered by total quantity purchased.

-- 25. Rank orders by the number of items in the order.

-- 26. For each employee, rank orders by Freight cost (highest first).

-- 27. Rank customers by the number of distinct products purchased.

-- 28. For each category, rank products by total number of times ordered.

-- 29. Rank shippers by total freight revenue.

-- 30. For each supplier, rank products by UnitPrice within the supplier.
