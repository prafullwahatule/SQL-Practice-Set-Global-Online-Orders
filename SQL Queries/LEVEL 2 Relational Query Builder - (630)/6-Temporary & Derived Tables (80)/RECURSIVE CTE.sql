-- =============================================
-- RECURSIVE CTE
-- Definition:
-- A Recursive CTE references itself to perform iterative calculations, hierarchy processing, or sequence generation.
-- Useful for sequences, cumulative totals, and hierarchical queries.
-- =============================================

-- 1. Use a recursive CTE to generate a sequence of numbers from 1 to 100.

-- 2. Use a recursive CTE to generate the last 12 months for monthly reports.

-- 3. Use a recursive CTE to calculate cumulative stock value per product category.

-- 4. Use a recursive CTE to calculate running total of orders per customer.

-- 5. Use a recursive CTE to generate all order dates between '1996-07-01' and '1996-07-31'.

-- 6. Use a recursive CTE to calculate cumulative quantity ordered per product.

-- 7. Use a recursive CTE to generate hierarchy levels of employees (if a ManagerID column existed).

-- 8. Use a recursive CTE to calculate cumulative sales per month.

-- 9. Use a recursive CTE to generate a list of 10 consecutive ProductIDs.

-- 10. Use a recursive CTE to calculate running total of UnitPrice per category.

-- 11. Use a recursive CTE to generate the next 5 business days from a given date.

-- 12. Use a recursive CTE to show cumulative orders per customer per month.

-- 13. Use a recursive CTE to calculate total stock for top 10 products recursively.

-- 14. Use a recursive CTE to generate all multiples of 5 up to 100.

-- 15. Use a recursive CTE to calculate cumulative number of orders per employee.

-- 16. Use a recursive CTE to generate a sequence of CustomerIDs from 1 to 91.

-- 17. Use a recursive CTE to calculate running total of UnitsInStock per category.

-- 18. Use a recursive CTE to list all order dates recursively for each customer.

-- 19. Use a recursive CTE to calculate cumulative revenue per product category.

-- 20. Use a recursive CTE to generate a list of all months in 1996 for reporting.
